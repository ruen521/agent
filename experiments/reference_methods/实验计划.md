# 实验计划（本阶段定义，不执行）

## 目标

1. 比较 Method 4-12 在当前真实数据上的预测效果。
2. 输出方法可用性与准确性（含参考文档指标与现代指标）。
3. 明确 Method 1/2/3 在当前数据上不可跑。

## 数据范围

1. 主样本：`experiment_sales_daily` 与 `experiment_inventory_snapshot_asin` 的 ASIN 交集。
2. 目标列：`units`。
3. 时间范围：`2025-11-06` 至 `2026-02-05`。

## 实验矩阵

1. `day + pbf=3`（主口径）
2. `week + pbf=3`（对照）
3. `day + pbf=14`（稳健性对照）

## 方法范围

1. 可跑：Method 4-12（Method 12 完整实现，`L=7`，不足时 `not_applicable`）。
2. 不可跑：Method 1/2/3（缺少完整去年同期历史）。

## 指标

1. 参考文档指标：`MAD`、`POA`
2. 现代指标：`MAE`、`WAPE`、`Bias`
3. 可用性：`runnable_asin_count`、`not_applicable_asin_count`、`coverage_rate`

## 输出物

1. `summary.json`
2. `method_metrics.csv`
3. `asin_method_metrics.csv`
4. `predictions.csv`
5. `实验报告.md`
